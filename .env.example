# Email Configuration for Contact Form (Hostinger SMTP)
# Configure these in Vercel Environment Variables

SMTP_HOST=smtp.hostinger.com
SMTP_PORT=465
MAIL_USER=Info@preuae.com
MAIL_PASS=your-email-password

# Server Port (optional, defaults to 3001 - not used on Vercel)
PORT=3001
